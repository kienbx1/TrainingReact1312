export const requiredMessage = 'Trường này không được để trống!'
export const invalidMessage = 'Giá trị không hợp lệ!'
export const minLengthMessage = (length) => {
  return `Ít nhất ${length} ký tự!`
}
export const maxLengthMessage = (length) => {
  return `Tối đa ${length} ký tự!`
}
